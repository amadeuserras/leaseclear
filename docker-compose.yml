services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: leaseclear
      POSTGRES_PASSWORD: leaseclear
      POSTGRES_DB: leaseclear
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
